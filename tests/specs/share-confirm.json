[
  {
    "name": "share button with unknown track shows unavailable message",
    "steps": [
      {
        "send": { "callback": "share:unknown123" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "That track isn't available anymore — run a new search to find it again."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "share confirmation yes shows shared message",
    "steps": [
      {
        "send": { "callback": "share_confirm:yes" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Nothing to share — start a new search."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "share confirmation no returns to menu",
    "steps": [
      {
        "send": { "callback": "share_confirm:no" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No worries — back to the menu."
            }
          }
        ]
      }
    ]
  }
]
