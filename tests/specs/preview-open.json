[
  {
    "name": "preview button with unknown track shows expired message",
    "steps": [
      {
        "send": { "callback": "preview:unknown123" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "That preview link has expired — run a new search to find it again."
            }
          }
        ]
      }
    ]
  }
]
